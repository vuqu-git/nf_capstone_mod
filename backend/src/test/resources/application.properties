#application.properties in the test folder:
#   Properties files in the src/test/resources directory are specifically for test configurations.
#   This means that the embedded MongoDB version you're setting (in line 7) will only affect your test environment, not your production or development environments.

# Flapdoodle Embedded MongoDB is a Java library that allows you to easily run an embedded, in-memory MongoDB instance within your Java integration tests.
# This property specifies the exact version of MongoDB that the Flapdoodle library should download and use for the embedded instance:
de.flapdoodle.mongodb.embedded.version=7.0.4

#connection to MySQL db
#spring.datasource.url=jdbc:mysql://mysql-pupille-pupilledb.k.aivencloud.com:12260/pupille
#spring.datasource.username=avnadmin
##spring.datasource.password=${MYSQL_DB_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
